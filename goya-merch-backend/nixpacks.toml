# Nixpacks config for the Medusa backend (Coolify deployment)
# Coolify Base Directory: goya-merch-backend
# Port: 9000

[variables]
NIXPACKS_NODE_VERSION = "20"
NPM_CONFIG_PRODUCTION = "false"

[phases.setup]
nixPkgs = ["...", "openssl"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["cd apps/backend && npx medusa build"]

[start]
cmd = "cd apps/backend && npx medusa build && npx medusa db:migrate && npx medusa start"