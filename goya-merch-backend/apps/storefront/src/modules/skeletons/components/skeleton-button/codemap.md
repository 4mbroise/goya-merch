# goya-merch-backend/apps/storefront/src/modules/skeletons/components/skeleton-button/

## Responsibility
Placeholder for a full-width button element (e.g., the checkout or submit button).

## Design
Minimal skeleton — single `div` with `w-full`, `min-h-[50px]`, and `bg-gray-100`. No props.

## Integration
Consumed by `SkeletonOrderSummary`.
